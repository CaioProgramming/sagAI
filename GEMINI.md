# Gemini CLI Instructions

- Always use `./gradlew installDebug` instead of `./gradlew assembleDebug` when testing or verifying
  features to ensure they are installed directly on the user's device for testing.
