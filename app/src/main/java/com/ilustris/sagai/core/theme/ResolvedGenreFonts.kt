package com.ilustris.sagai.core.theme

import androidx.compose.ui.text.font.FontFamily

/** Remote or cached typography for a [Genre]. */
data class ResolvedGenreFonts(
    val header: FontFamily,
    val body: FontFamily,
)
